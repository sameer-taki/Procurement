import React from 'react'

// Phase 1: Claude Code builds the real shell here — auth gate, nav, and the
// Dashboard + Stock screens. This placeholder just confirms the build works.
export default function App() {
  return (
    <main style={{ fontFamily: 'system-ui', padding: 24 }}>
      <h1>Golden Procurement</h1>
      <p>Scaffold OK. Build the app per CLAUDE.md (start with Phase 1: Stock view).</p>
    </main>
  )
}
